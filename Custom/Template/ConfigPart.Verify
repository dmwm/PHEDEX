#### Agent to verify data consistency on storage

### AGENT LABEL=blockverify PROGRAM=Toolkit/Verify/BlockDownloadVerify
 -db              ${PHEDEX_DBPARAM}  
 -nodes           ${PHEDEX_NODE},${PHEDEX_MSS}
 -namespace       'posix'
