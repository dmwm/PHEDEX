#### Export-side agents

### AGENT LABEL=exp-pfn PROGRAM=Toolkit/Transfer/FileExport
 -db          ${PHEDEX_DBPARAM}
 -state       ${PHEDEX_STATE}
 -nodes       ${PHEDEX_NODE}
 -ignore      ${PHEDEX_MSS}
 -storagemap  ${PHEDEX_MAP}
