#### Download agents

#### Be careful with FileDownloadDelete on Castor-2 based
#### systems. Castor sites, please follow the suggestion in
#### README-Transfer on CVS.


### AGENT LABEL=download-srm PROGRAM=Toolkit/Transfer/FileDownload
 -db              ${PHEDEX_DBPARAM}
 -nodes           ${PHEDEX_BUFFERNODE}
 -storagemap      ${PHEDEX_MAP}
 -delete          ${PHEDEX_CONFIG}/FileDownloadDelete
 -validate        ${PHEDEX_CONFIG}/FileDownloadSRMVerify
 -backend         SRM
 -command         srmcp,-x509_user_proxy=${PHEDEX_PROXY}
 -jobs            10
 -batch-files     50

### AGENT LABEL=download-lt-push PROGRAM=Toolkit/Transfer/FileDownload ENVIRON=glite
 -db              ${PHEDEX_DBPARAM}
 -nodes           ${PHEDEX_BUFFERNODE}
 -storagemap      ${PHEDEX_MAP}
 -delete          ${PHEDEX_CONFIG}/FileDownloadDelete
 -validate        ${PHEDEX_CONFIG}/FileDownloadVerify
 -backend         SRM
 -command         ${PHEDEX_SCRIPTS}/Utilities/ftscp,-passfile=${PHEDEX_CONFIG}/ftspass,-server=${PHEDEX_FTS_SERVER}
 -jobs            10
 -batch-files     50
