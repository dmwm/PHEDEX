#### General configuration
### ENVIRON common
PHEDEX_BASE=<path to your PhEDEx base dir e.g.: /home/phedex>;
PHEDEX_CONFIG=${PHEDEX_BASE}/SITECONF/<YourSite>/PhEDEx
PHEDEX_DBPARAM=${PHEDEX_CONFIG}/DBParam:<instance>/<site>;
PHEDEX_STATE=${PHEDEX_BASE}/<purpose>_state;
PHEDEX_LOGS=${PHEDEX_BASE}/<purpose>_logs;
PHEDEX_SCRIPTS=${PHEDEX_BASE}/sw/slc3_ia32_gcc323/cms/PHEDEX/PHEDEX_<PhEDExVersion>;
PHEDEX_MAP=${PHEDEX_CONFIG}/Storage.xml;
PHEDEX_NODE=T<x>_<Sitename>
PHEDEX_BUFFERNODE=${PHEDEX_NODE}_Buffer
PHEDEX_MSSNODE=${PHEDEX_NODE}_MSS

export X509_USER_PROXY=<Location of your Grid proxy file>;

# Source environment
source ${PHEDEX_SCRIPTS}/etc/profile.d/env.sh

### ENVIRON glite
source <Path to your GLite environemnt>/etc/profile.d/grid_env.sh;
export GLITE_SD_PLUGIN=bdii;
PHEDEX_FTS_SERVER=$(glite-sd-query -e -t org.glite.FileTransfer -s <Your GliteTransferEndpoint> \
                    | grep Endpoint | awk '{print $2}');

### IMPORT ConfigPart.Common
### IMPORT ConfigPart.Export
### IMPORT ConfigPart.Download
### IMPORT ConfigPart.MSS
