for f in rls tmdb rls-replica xml meta pin clean rank; do
  grep stats logs/$f |
    awk '{print $6,$7,$8}' |
    tr -d rus | tr ' ' '	' |
    paste =(grep stats logs/$f |
  	    cut -d: -f1-3) - > stats-$f-`date +%Y%m%d%H%M`.txt
done
